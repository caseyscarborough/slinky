<nav class="navbar">
  <div class="navbar-inner">
    <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </a>
    <a class="brand" href="/">Slinky<sup>(beta)</sup></a>
    <div class="nav-collapse">
      <ul class="nav pull-right">
        <% if signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <small><%= gravatar_for(current_user, '24', 'img-circle') %> Welcome, <%= current_user.first_name %>!</small>
              <i class="caret"></i>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Dashboard", dashboard_path, :html => { :class => 'user-link' } %></li>
              <li><%= link_to "Edit Profile", profile_path, :html => { :class => 'user-link' } %></li>
              <li><%= link_to "New Link", new_link_path, :html => { :class => 'user-link' } %></li>
            </ul>
          </li>
        <% end %>
        <li><%= link_to "Home", '/' %></li>
        <% if signed_in? %>
          <li><%= link_to "Logout", logout_path%></li>
        <% else %>
          <li><%= link_to "Login", login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>